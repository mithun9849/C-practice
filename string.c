#include <stdio.h>

int main() {
    char str[20];
    printf("Enter ur name:");
    fgets(str,sizeof(str),stdin);
    printf("%s",str);
    

    return 0;
}

stdin = "standard input"
str = name of string
